const PREFIX = '/api';

export const coursePrefix = `${PREFIX}/course`;
export const documentPrefix = `${PREFIX}/document`;
export const userPrefix = `${PREFIX}/user`;
export const videoPrefix = `${PREFIX}/video`;
export const questionPrefix = `${PREFIX}/question`;
